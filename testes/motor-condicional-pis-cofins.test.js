const assert=require('assert'),fs=require('fs'),os=require('os'),path=require('path');process.env.SATTVA_DADOS=fs.mkdtempSync(path.join(os.tmpdir(),'sattva-cond-'));const db=require('../src/db'),cfc=require('../src/services/cadastroFiscalComplementar'),m=require('../src/services/motorCondicionalPisCofins');const e=db.prepare("insert into empresas(cnpj,razao_social) values('00000000000003','Teste')").run().lastInsertRowid;const c={empresa_id:Number(e),codigo_produto:'X',ncm:'NCM_TESTE'};const a={regra_id:'A',prioridade:2,condicoes:[{fato:'revendedor',operador:'VERDADEIRO'}],resultado:{tratamento:'A'}};const b={regra_id:'B',prioridade:1,condicoes:[{fato:'fabricacao_propria',operador:'VERDADEIRO'}],resultado:{tratamento:'B'}};assert.equal(m.selecionar([a,b],{...c,fatos_documento:{revendedor:true}}).selecionada.regra.regra_id,'A');cfc.salvarFato({...c,fato:'revendedor',valor:'SIM'});assert.equal(m.selecionar([a],c).status,'APLICAVEL');assert.equal(m.selecionar([{...a,regra_id:'ZERO',condicoes:[{fato:'defensivo_agropecuario',operador:'VERDADEIRO'}],resultado:{pis:0,cofins:0}}],c,{criar_pendencia:true}).status,'INDETERMINADA');assert.equal(m.sombra({oficial:{pis:1},regras:[a],contexto:c}).gravou_producao,false);console.log('motor-condicional: 4 cenários aprovados');db.close();fs.rmSync(process.env.SATTVA_DADOS,{recursive:true,force:true});
